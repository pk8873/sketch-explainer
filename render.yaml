services:
  - type: web
    name: sketch-explainer
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: 10000
    disk:
      name: output
      mountPath: /app/output
      sizeGB: 1
